[package]
name = "dbgflow-mcp"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
dbgflow-core = { path = "../dbgflow-core" }
serde.workspace = true
serde_json.workspace = true

[target.'cfg(windows)'.dependencies]
windows-service = "0.7"
